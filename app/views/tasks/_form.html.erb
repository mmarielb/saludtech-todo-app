<!-- formulario reutilizable para crear o editar tareas. -->
<%= form_with(model: @task, local: true) do |f| %>
  <div class="mb-3">
    <%= f.label :title %>
    <%= f.text_field :title, class: "form-control", required: true %>
  </div>

  <%= f.submit "Actualizar Tarea", class: "btn btn-magenta" %>
<% end %>